server:
  port: 8089
 
spring:
  application:
    name: ORDER-SERVICE

logging:
  file:
    name: D:\logs\ZuulAPP.log
    
management:
  endpoints:
    web:
      exposure:
        include: "*"

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"